<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="p2_vaio.jiit">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@android:mipmap/sym_def_app_icon"
        android:label="Cyber Shrishti-17"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Cyber Shrishti&apos;17"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".hackathon" />
        <activity android:name=".execute" />
        <activity android:name=".develop" />
        <activity android:name=".debate" />
        <activity android:name=".automation" />
        <activity android:name=".circuit" />
        <activity android:name=".startup" />
        <activity android:name=".debug" />
        <activity android:name=".jpl" />
        <activity android:name=".photoshop" />
        <activity android:name=".paper" />
        <activity android:name=".creative" />
        <activity android:name=".project" />
        <activity android:name=".bio" />
        <activity android:name=".codeofcodons" />
        <activity android:name=".hydrophilic" />
        <activity android:name=".registration" />
        <activity android:name=".Event" />
        <activity android:name=".FBpage" />
        <activity android:name=".Contact_us" />
        <activity android:name=".AboutUs" />
        <activity android:name=".Accomodationform" />


        <service android:name=".MyFirebaseInstanceIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>

            <service android:name=".MyFirebaseMessagingService">
                <intent-filter>
                    <action android:name="com.google.firebase.MESSAGING_EVENT" />
                </intent-filter>
            </service>
        </service>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".sustainability"></activity>
    </application>

</manifest>